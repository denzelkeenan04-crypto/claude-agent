name: Claude Agent

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Get Notion tasks
        run: |
          curl -X POST "https://api.notion.com/v1/databases/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/query" \
          -H "Authorization: Bearer ${{ secrets.NOTION_TOKEN }}" \
          -H "Notion-Version: 2022-06-28" \
          -H "Content-Type: application/json"
